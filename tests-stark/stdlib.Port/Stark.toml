[project]
name = "stdlib-port-tests"
version = "0.1.0"
kind = "test"

[test]
root = "StdlibPortTests.stark"
output = "stdlib-port-tests"

[dependencies]
stdlib = { path = "../../stdlib" }

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
