[project]
name = "selfhost-pipeline-tests"
version = "0.1.0"
kind = "test"

[test]
root = "PipelineTests.stark"
output = "selfhost-pipeline-tests"

[dependencies]
stark-compiler = { path = "../../selfhost" }

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
