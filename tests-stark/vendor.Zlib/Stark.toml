[project]
name = "vendor-zlib-tests"
version = "0.1.0"
kind = "test"

[test]
root = "ZlibTests.stark"
output = "vendor-zlib-tests"

[dependencies]
stdlib = { path = "../../stdlib" }

[native]
sources = ["../../vendor/ZlibStreamBinding.c"]
pkg-config = ["zlib"]

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
