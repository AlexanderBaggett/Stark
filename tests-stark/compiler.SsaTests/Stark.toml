[project]
name = "compiler-ssa-tests"
version = "0.1.0"
kind = "test"

[test]
root = "SsaTests.stark"
output = "compiler-ssa-tests"

[dependencies]
stdlib = { path = "../../stdlib" }

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
