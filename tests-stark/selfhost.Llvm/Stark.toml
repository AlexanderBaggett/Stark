[project]
name = "selfhost-llvm-tests"
version = "0.1.0"
kind = "test"

[test]
root = "LlvmTests.stark"
output = "selfhost-llvm-tests"

[dependencies]
stark-compiler = { path = "../../selfhost" }

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
