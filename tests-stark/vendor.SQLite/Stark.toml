[project]
name = "vendor-sqlite-tests"
version = "0.1.0"
kind = "test"

[test]
root = "SQLiteTests.stark"
output = "vendor-sqlite-tests"

[native]
sources = ["../../vendor/SQLiteTextBinding.c"]
pkg-config = ["sqlite3"]

[native.fallback.macos]
include-dirs = ["../../vendor/dist/arm64-apple-macosx26.0.0/native/sqlite"]
library-dirs = ["../../vendor/dist/arm64-apple-macosx26.0.0/native/sqlite"]
libraries = ["sqlite3"]

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
