# Pending Corpus Files

Corpus files whose shapes stage1's whole-module path (`CompileModuleFromAst`)
does not accept yet. Each is a stage0-validated program (distinct exit code)
that moves up one directory — and joins the StageParityTests gate — the day
its blocking family lands.

No pending corpus files are currently listed.

Tracked in docs/Self-host-Prep/TASKS.md §1.
