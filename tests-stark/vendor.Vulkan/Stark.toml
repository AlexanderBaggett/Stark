[project]
name = "vendor-vulkan-tests"
version = "0.1.0"
kind = "test"

[test]
root = "VulkanTests.stark"
output = "vendor-vulkan-tests"

[dependencies]
stdlib = { path = "../../stdlib" }

[native]
pkg-config = ["vulkan"]

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
