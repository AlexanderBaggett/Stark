[project]
name = "vendor-curl-tests"
version = "0.1.0"
kind = "test"

[test]
root = "CurlTests.stark"
output = "vendor-curl-tests"

[dependencies]
stdlib = { path = "../../stdlib" }

[native]
sources = ["../../vendor/CurlEasyBinding.c"]
pkg-config = ["libcurl"]

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
