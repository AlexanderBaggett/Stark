[project]
name = "selfhost-ir-tests"
version = "0.1.0"
kind = "test"

[test]
root = "IrTests.stark"
output = "selfhost-ir-tests"

[dependencies]
stark-compiler = { path = "../../selfhost" }
stdlib = { path = "../../stdlib" }

[profiles.dev]
opt = 0

[profiles.release]
opt = 3
