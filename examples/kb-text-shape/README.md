# KbTextShape Example

Build the bundled text shaping vendor package first:

```bash
bash vendor/build-kb-text-shape-package.sh
```

Then run this example with the Stark project command from this directory. It
uses the checked-in Vera font fixture from `vendor/native/stb/testdata`.
