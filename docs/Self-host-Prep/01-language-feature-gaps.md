# Phase 1 - Language Feature Gap Analysis

Severity meanings:

- `blocker`: port cannot be kept faithful without this feature or a deliberate
  whole-port rewrite convention.
- `workaround-exists`: the compiler can be ported with more verbose Stark.
- `nice-to-have`: improves maintainability but does not block self-hosting.

Design states:

- `specified`: documented language contract exists.
- `partially implemented`: grammar/compiler support exists but is incomplete for
  self-hosting use.
- `unspecified`: no settled Stark surface or port convention found.

Scheduling note: L09's broad `comptime` inventory is historical evidence, not a
current pre-self-host task list. The pre-self-host baseline is frozen in
`26-comptime-pre-self-host-scope.md`; broad evaluator parity, future
`System.Compiler` expansion, and broad package/source preservation are deferred
to `27-comptime-post-self-host-scope.md`.

## Gap Table

| ID | Feature / Capability | Host Compiler Use | Severity | Design State | Audit Finding |
|---|---|---|---|---|---|
| L01 | Error-value propagation for `Result` / `Option` style code | Host uses `throw`, `try`, `catch`, nullable returns, and `InvalidOperationException` in `src/Compiler/CompilerPipeline.cs`, `src/Compiler/NativeToolchain.cs`, `src/Compiler/CompilerCli.cs`, parser helpers, package loading | blocker | **implemented (v2)** | **Closed (v2, doc `11`):** `try expr` (visible leading-keyword early return, statement-boundary positions) unwraps the `[Ok]` variant and propagates the `[Err]` variant of any **two-variant enum carrying `[Ok]`/`[Err]` role attributes** — structural, no privileged stdlib types, no name matching; differing error payloads convert through the enclosing error type's `from` funnel (declared once on the error enum), and a missing funnel is a compile error. Stdlib result/status enums are annotated; cross-family propagation (e.g. stdlib `IOResult` operand inside a user result-returning function) works through funnels; role/misuse diagnostics STK3037–STK3043; runtime + diagnostics tests and user-facing docs cover the role model. Cross-module package-image carry of roles/funnels remains open as EP15. |
| L02 | Pattern binding in branch conditions (`if let`, `while let`) | C# relies on `is { } value`, nullable pattern checks, `switch` expressions, and guard-heavy branching throughout `TypeChecking.cs`, `SemanticValidation.cs`, package loaders | workaround-exists | **implemented** | **Closed by `is`-pattern conditions:** `if (expr is pattern)` and `while behavior (expr is pattern)` reuse the `switch case` pattern grammar/binder verbatim — full enum/nested/struct/named-field/`var`/`_`/literal coverage — binding captures into the consequent (then-branch / loop body). Lowers to the same discriminant-test-and-bind machinery as a single-case `switch` (no extra cost), with identical move-out-of-scrutinee and drop semantics. Maps the host's `x is Foo f` directly. |
| L03 | Explicit traversal loops | Host uses `foreach`, LINQ, `IEnumerable<T>`, `yield return` in passes and helpers such as `CompilerPipeline.cs`, `ParameterMemoryContractFacts.cs`, `NativeToolchain.cs` | workaround-exists | **implemented** | **Closed (doc `08`):** Stark now supports exactly three explicit `for ... in ...` traversal loop forms before self-hosting: borrowed element traversal, mutable borrowed element traversal, and indexed borrowed traversal. The implementation supports fixed arrays, slices, and dynamic storage, lowering to counted loops over existing element-address operations with no iterator objects, `yield`, hidden allocation, LINQ-style API, or runtime dispatch. Collection APIs still need fast explicit count/index/slice access for hot compiler paths and for cases outside the three forms. |
| L04 | Rich switch patterns: or-patterns, range patterns, list patterns, property patterns | C# source uses switch/pattern matching heavily in validators, lowering, text rendering, package codecs, and generated parser interaction | workaround-exists | **implemented** | Stark supports literal, discard, `var`, enum case, generic enum case, named-field enum, aggregate patterns, switch-label or-pattern alternatives (`case A \| B:`), inclusive integer range patterns (`case 0..10:`), aggregate property patterns (`case Box { Field: pattern }:`), and exact-length list patterns over fixed arrays/slices/dynamic storage (`case [first, second]:`). Or-pattern alternatives that bind captures must bind the same names with the same types, or the section is a compile-time error. Range patterns use interval coverage and lower without case expansion. Property patterns require every aggregate field exactly once. List patterns are exact-length; fixed-array length mismatches are compile-time errors, while slice/dynamic list patterns lower to a length check plus direct element tests. Typed package-image templates preserve range, property, and list pattern facts across module boundaries. |
| L05 | Generic value parameters / const generics | Host models fixed arrays, bounded raw pointers, target/layout facts, and template bodies; stdlib and tests would benefit from generic fixed-size helpers | workaround-exists | **implemented (integer slice)** | **Decision closed (OQ-22, docs `08` and `13`):** add typed `comptime` generic value parameters as Stark's const-generic spelling, e.g. `struct FixedBuffer<T, comptime u64[0 max] N>`. The self-hosting range-typed integer slice has landed: parser support, typed signature/named-type metadata, symbolic fixed-array lengths (`T[N]`), fixed-array argument inference, range diagnostics, explicit integer value arguments at type/function call sites (`Length<i32[min max], 3>(values)`), symbolic forwarding with `comptime N`, function monomorphization keys, scalar body materialization (`return N`), type-reference package-image preservation for symbolic lengths, imported-template value substitution, and full package/source-surface metadata for comptime parameter declarations. Non-integer comptime value kinds are deferred until a concrete compiler or stdlib use case needs them. |
| L06 | General operator/hash/equality doctrine surface for generic algorithms | Host relies on .NET string equality, `StringComparer`, generic hashing, numeric operators, comparison/sort helpers, dictionaries/sets throughout `TypeChecking.cs`, `ProjectCliDriver.cs`, package builders/loaders | blocker | partially implemented | **Design closed by OQ-08 and doc `19`:** generic collections and algorithms use explicit `Hash` + `Eq`, `Ord`, and `Format` contracts as the public language/stdlib model; the compiler uses strongly typed interned IDs as its hot-path representation. Traits are implemented (see `10-traits-and-dynamic-dispatch.md`): impl via base list, `Self`, conformance, default members, associated types, and `where T: Trait` constrained generics, all statically dispatched to direct calls. Landed: canonical contract names, explicit static `Dictionary<K,V>`/`HashSet<T>` key `Hash`/`Equals` support for non-primitive key types, scalar fast paths for bool/integer dictionary/set keys, and exact ordinal `ascii`/`unicode` plus owned-text `Hash`/`Eq`/`Ord`/`Format` helpers. Remaining work is stdlib/collections coverage: ordered/deterministic collection paths, borrowed lookups, typed compiler interners, and missing-contract diagnostics. |
| L07 | Compiler invariant failure policy (`panic` / `trap` / abort) | Host throws for impossible states: missing artifacts in `CompilerPipeline.cs`, unsupported accepted bodies in LLVM/MIR paths, toolchain failures in `NativeToolchain.cs` | blocker | **closed (no new surface)** | **Resolved without adding panic/trap/assert (OQ-05, doc `09` Error Model):** invalid states are made unrepresentable instead. Switch exhaustiveness (STK3044) and definite-return analysis (STK3045) turn unmatched-value and fall-off-the-end paths into compile errors; toolchain/environment failures port as `Result` + `try`; the residual self-detected-bug category reports via `Result<T, InternalError>` or stderr + `System.Process.Exit(1)`. Port convention documented in doc `09`. |
| L08 | Raw/multiline/interpolated string literal ergonomics for compiler text | Host builds LLVM IR, diagnostics, source snippets, JSON, and long test source strings with C# raw/interpolated strings and `StringBuilder` | workaround-exists | **implemented (language literal slice)** | **Closed for source literals:** `Stark.g4` accepts exact-preserving `raw"..."` and multiline `raw"""..."""` as ordinary `StringLiteral` tokens, and `$raw"..."` / `$raw"""..."""` compose with existing interpolation. Escapes are not interpreted inside raw literals; multiline content is preserved exactly between delimiters. Remaining text-builder and escaping APIs are stdlib gap S02/S03, not a source-literal syntax gap. |
| L09 | General compile-time function evaluation and program-structure branching | Host uses `CompileTimeExpressionEvaluator.cs`, `IntegerRangeStorageFacts.cs`, enum layout, SSA const lookup-table folding, text/path specializers, typed package facts, ABI/layout facts, trait/doctrine facts, and compiler metadata builders | workaround-exists | partially implemented | **Decision closed (doc `13`; current-host audit in `docs/internal/ctfe-self-host-compiler-audit.md`):** `comptime` scope is CTFE plus broad compile-time branching over explicit program-structure facts. Landed slice: unary `comptime expr` syntax; value-position `comptime` blocks; type-check enforcement; zero-generic declared `finite` / `law` CTFE calls over literals, const values, comptime generic values, ordinary expressions, local constants/variables, mutable CTFE local assignment, boolean `if`, ordinary `switch` statements over literals/ranges/or-labels/fixed-array list patterns/aggregate property and positional patterns/enum unit, tuple, and named-field patterns/default labels/`when` guards, pattern-condition `if (expr is pattern)` and `while willexit (expr is pattern)` over the same CTFE pattern subset, `return`, bounded `willexit` `while` / classic `for`, `break`, and `continue`; fixed-array aggregate CTFE through local initializers, returns, constant indexing, mutable element assignment for table generation, typed const globals, and MIR aggregate materialization; named struct/record aggregate CTFE through object creation, object initializers, primary record constructors, field projection, local const tracking, typed const globals, direct MIR materialization, LLVM static initializers, and typed package-image preservation; enum aggregate CTFE through unit values, tuple-like constructor calls, named-field enum constructors, enum equality in CTFE, typed const globals, direct MIR materialization through enum layout facts, LLVM static initializers, and typed package-image preservation; concrete layout-query CTFE for `sizeof(T)` and `alignof(T)` over concrete runtime layouts in unary `comptime`, value-position blocks, declared finite/law calls, and typed const globals; first structural-fact slices with compiler-known `System.Compiler.Is*<T>()` / `HasConcreteLayout<T>()` type predicates, type-level concrete layout facts (`TypeSize`, `TypeAlign`, `TypeIsZeroSized`), scalar type metadata facts (`TypeIntegerBitWidth`, `TypeFloatBitWidth`, `TypeIntegerIsSigned`, `TypeIntegerIsUnsigned`, `TypeIntegerIsFullRange`, `TypeIntegerMinIs`, `TypeIntegerMaxIs`), raw-pointer metadata facts (`RawPointerElementTypeIs`, `RawPointerElementTypeIs*`, `RawPointerElementTypeHasConcreteLayout`, `RawPointerIsMutable`, `RawPointerIsReadOnly`), element-bearing type and fixed-array length metadata facts (`TypeElementTypeIs`, `TypeElementTypeIs*`, `TypeElementTypeHasConcreteLayout`, `TypeFixedArrayLength`, `TypeFixedArrayLengthIs`), top-level type qualifier metadata facts (`TypeHasQualifiers`, borrow/access/init kind predicates, `TypeIsMutableView`, `TypeUnqualifiedTypeIs`), `System.Compiler.FieldCount<T>()` / `EnumVariantCount<T>()` count facts, indexed field layout facts (`FieldOffset`, `FieldSize`, `FieldAlign`, `FieldIsMisaligned`), layout-control attribute facts (`StructLayoutIsAuto`, `StructLayoutIsC`, `StructLayoutIsExplicit`, `StructHasPack`, `StructPack`, `StructHasAlign`, `StructAlign`, `FieldHasExplicitOffset`, `FieldExplicitOffset`), indexed enum variant facts (`EnumVariantPayloadCount`, `EnumVariantIsOk`, `EnumVariantIsErr`, `EnumVariantIsErrorFunnel`), enum payload layout facts (`EnumVariantPayloadOffset`, `EnumVariantPayloadSize`, `EnumVariantPayloadAlign`, `EnumVariantPayloadIsMisaligned`), enum discriminant/tag facts (`EnumVariantTag`, `EnumTagOffset`, `EnumTagSize`, `EnumTagAlign`, `EnumTagIsMisaligned`), indexed exact type facts (`FieldTypeIs`, `EnumVariantPayloadTypeIs`, `EnumVariantAbsorbsErrorTypeIs`), indexed field/payload type-category facts (`FieldTypeIs*`, `FieldTypeHasConcreteLayout`, `EnumVariantPayloadTypeIs*`, `EnumVariantPayloadTypeHasConcreteLayout`), function-pointer signature/kind/ABI/memory-contract facts (`FunctionPointerParameterCount`, `FunctionPointerReturnTypeIs`, `FunctionPointerParameterTypeIs`, `FunctionPointerReturnTypeIs*`, `FunctionPointerParameterTypeIs*`, `FunctionPointerKindIs*`, `FunctionPointerIsUnsafe`, `FunctionPointerHasFfiAbi`, `FunctionPointerAbiIs*`, `FunctionPointerParametersAreDisjoint`, `FunctionPointerParametersOverlap`, `FunctionPointerParametersAreSame`), closure signature/kind/storage/call-capability/memory-contract facts (`ClosureParameterCount`, `ClosureReturnTypeIs`, `ClosureParameterTypeIs`, `ClosureReturnTypeIs*`, `ClosureParameterTypeIs*`, `ClosureKindIs*`, `ClosureStorageIsBorrow`, `ClosureStorageIsHeap`, `ClosureStorageIsInline`, `ClosureCallCapabilityIsNormal`, `ClosureCallCapabilityIsMut`, `ClosureCallCapabilityIsOnce`, `ClosureParametersAreDisjoint`, `ClosureParametersOverlap`, `ClosureParametersAreSame`), dyn-trait metadata facts (`IsDynTrait`, `DynTraitIsView`, `DynTraitIsHeap`, `DynTraitTargetTypeIs`), named/generic type metadata facts (`TypeDisplayName`, `TypeBaseName`, `TypeIsGenericInstantiation`, `TypeArgumentCount`, `TypeComptimeArgumentCount`, `TypeArgumentTypeIs`, `TypeArgumentTypeIs*`, `TypeArgumentTypeHasConcreteLayout`), named-type method metadata facts (`MethodCount`, `MethodName`, `MethodParameterCount`, `MethodReturnTypeIs`, `MethodParameterTypeIs`, `MethodReturnTypeIs*`, `MethodParameterTypeIs*`, `MethodKindIs*`, `MethodIsStatic`, `MethodHasBody`, `MethodIsUnsafe`, `MethodIsVarargs`, `MethodHasFfiAbi`, `MethodAbiIs*`, `MethodParametersAreDisjoint`, `MethodParametersOverlap`, `MethodParametersAreSame`, `MethodGenericParameterCount`, `MethodGenericParameterName`, `MethodComptimeGenericParameterCount`, `MethodComptimeGenericParameterName`, `MethodComptimeGenericParameterTypeIs`), indexed name facts (`FieldName`, `EnumVariantName`), enum payload named-field facts (`EnumVariantUsesNamedFields`, `EnumVariantPayloadHasName`, `EnumVariantPayloadName`), associated-type facts (`AssociatedTypeCount`, `AssociatedTypeName`, `AssociatedTypeHasTarget`, `AssociatedTypeTargetTypeIs`, `AssociatedTypeTargetTypeIs*`, `AssociatedTypeTargetTypeHasConcreteLayout`), and trait-conformance fact `System.Compiler.Implements<T, Trait>()`; STK3054 runtime-use/out-of-range/wrong-trait-argument rejection, generic function/law CTFE substitution for instantiated type and range-typed integer `comptime` parameters, declared static and receiver `finite`/`law` method-call CTFE with borrow-aware parameter binding, chained receiver-call folding, and trait-default receiver fallback, open nested structural fact deferral during generic body type checking, and erasure before MIR/codegen; STK3053 diagnostics for runtime-only/unsupported expressions or block bodies, CTFE loop-budget exhaustion, and recursive compile-time function calls; typed const-global materialization; scalar MIR erasure and scalar const-global read folding. Remaining work: aggregate CTFE beyond the scalar/fixed-array/table/named-aggregate/enum/layout-query/switch/pattern-condition slices, richer structural-fact queries/branching over field attributes and richer field-type metadata beyond category/exact type comparison, enum payload attributes and richer payload metadata beyond category/names/exact type comparison/counts/roles/funnels/layout/tag facts, named top-level function declarations, attributes, doctrine facts beyond method metadata, callable metadata beyond the landed function-pointer, closure, dyn-trait, and method metadata slices, richer associated-type facts beyond the landed count/name/target/exact/category slice, richer type facts beyond the landed predicate/count/name/generic-instantiation/actual-argument/concrete-layout/scalar-metadata/raw-pointer-metadata/element-type/fixed-array-length/type-qualifier metadata slices, richer ABI/layout facts beyond the landed layout-control attribute and type-level layout fact slices, and package metadata, plus structural-fact erasure for future richer facts. |
| L10 | Async/build-driver concurrency replacement | `src/Compiler/ProjectCliDriver.cs` and `src/Compiler/CompilerCli.cs` expose `async Task<int>` and use async stream/process APIs | workaround-exists | **closed as porting convention** | The host compiler is functionally single-threaded: zero parallelism (`Task.WhenAll`/`Parallel`/threads — none); every `await` is sequential I/O (console/file writes, process wait). A synchronous Stark port is behaviorally identical, so self-hosting does not need `async`/`await`. OQ-11 resolves future parallel build/test coordination as a narrow stdlib scope in doc `22`: explicit payload thread starts, `System.Threading.Synchronized<T>` / `Locked<T>`, and MPSC channels. |
| L11 | Nullability and optional-value conventions | Host nullable references (`string?`, `Type?`, `TryGet(... out T?)`) are pervasive in type/model queries | blocker | **closed (convention specified; stdlib type implemented)** | Safe Stark has no nullable safe references; raw `null` is only for raw pointers and FFI. The blessed porting convention is `System.Option<T>` with `[Ok] Some(T)` / `[Err] None`. C# nullable returns, nullable fields, and `TryGet(... out T?)` become `Option<T>` values; callers inspect them with `switch` / `is Option<T>.Some(var value)` or propagate absence with `try` when the enclosing return type is also option-shaped. Project-local option-shaped enums remain legal under the structural `[Ok]`/`[Err]` role model, but `System.Option<T>` is the default self-hosting pattern. |
| L12 | Partial/nested/generated type layout ergonomics | Host uses generated partial parser classes, nested parse contexts, nested helper classes, nested records, and partial lowering/emitter classes | workaround-exists | partially implemented | Stark supports modules and top-level data declarations, but no evidence of partial type declarations or nested named types. The self-hosted parser will not port generated ANTLR parse-context classes; the handwritten parser should expose a Stark-native token/tree facade, while other nested helper shapes can be flattened into modules or explicit records. |
| L13 | Alias/noalias proof carriers and wrong-alias diagnostics | Host tracks `ScopedNoAliasGroup`, memory contracts, root keys, `if disjoint(...)`, `unsafe assume disjoint(...)`, and validates call-site disjointness in `TypeChecking.cs` before LLVM emission | blocker | **implemented** | **Decision closed (OQ-17, doc `09`):** APIs that need alias facts use explicit compile-time-only proof carriers, those carriers erase before codegen, and wrong proof use is rejected during type/lowering/SSA validation. `unsafe assume disjoint(...)` remains the only way to introduce an external disjointness fact, and it performs explicit root checks. Landed implementation: declaration memory contracts preserve `disjoint`/`overlap`/`same` groups in package images; runtime `if disjoint(...)` and trusted `assume disjoint(...)` lower to typed `AliasProofCarrier` records on scoped noalias SSA groups; SSA validation reports malformed proof ids, duplicate/blank roots, invalid root-key shapes, root/group mismatches, and unknown parameter roots before LLVM emission. |
| L14 | Parser generator/runtime strategy | Host parser is ANTLR-generated C# from `Stark.g4`, with runtime dependency and generated parse-context classes | blocker | **specified (handwritten parser)** | **Decision closed:** `Stark.g4` remains the canonical grammar reference, but the self-hosted compiler will use a handwritten parser rather than porting ANTLR runtime/generated code. Parser conformance tests should compare the handwritten parser against the canonical grammar and, while the host exists, the current C# ANTLR parser as an oracle. Tracked in tooling as T01. |

L09 update: callable bounded raw-pointer count-expression structural facts now
fold in CTFE for function pointers, closures, and methods:
`FunctionPointerParameterHasRawPointerElementCountExpression`,
`FunctionPointerParameterRawPointerElementCountExpression`,
`ClosureParameterHasRawPointerElementCountExpression`,
`ClosureParameterRawPointerElementCountExpression`,
`MethodParameterHasRawPointerElementCountExpression`, and
`MethodParameterRawPointerElementCountExpression`.

L09 scheduling update: the broad `comptime` / `System.Compiler` inventory is no
longer a pre-self-host blocker. The pre-self-host baseline is frozen at the
currently implemented host capability and tracked in
`26-comptime-pre-self-host-scope.md`; broad evaluator parity, future structural
fact expansion, and broad package/source preservation are deferred to
`27-comptime-post-self-host-scope.md`.

L09 update: callable return/parameter qualifier structural facts now fold in
CTFE for function pointers, closures, and methods. The covered qualifier facts
mirror top-level `Type*` qualifier metadata: qualifier presence, borrow kind,
access kind, initialization kind, mutable view, and unqualified type comparison.

L09 update: field and enum-payload qualifier structural facts now fold in CTFE
for declaration-contained types, including package-backed typed interfaces. The
covered facts mirror top-level `Type*` qualifier metadata: qualifier presence,
borrow kind, access kind, initialization kind, mutable view, and unqualified
type comparison.

L09 update: C source alias identity structural facts now fold in CTFE for
closure return and parameter types:
`ClosureReturnTypeHasCSourceAlias`, `ClosureReturnTypeCSourceAliasName`,
`ClosureParameterTypeHasCSourceAlias`, and
`ClosureParameterTypeCSourceAliasName`.

## Features Verified Present Enough For Porting

| Feature | Evidence | Remaining Caveat |
|---|---|---|
| `struct`, `record`, `enum` | `Stark.g4`, `docs/Userfacing/LanguageReference.md` sections 8.1-8.4 | Porting records/classes still needs ownership/storage choices |
| Generic type parameters | `Stark.g4` `typeParameterList`, Language Reference 6.5 | Type-only generics, no const/value parameters |
| Function kinds | Grammar supports `fn`, `finite`, `law`, `finite law`; pipeline derives function effects | Port must preserve effect-refinement artifacts |
| Function pointers and closures | Language Reference 5.5-5.7 | Heap closure support and shared capture need careful stdlib/runtime review |
| Type aliases | Grammar `alias`, `TypeAliasSymbol`, `StarkTypeResolver` | Type aliases do not create distinct runtime alias classes |
| Borrow classes | `borrow`, `retborrow`, `storeborrow`, `frozen`, `shared`, `const` in grammar/docs | Port must avoid using C# shared references as a design crutch |
| Dynamic storage | Language Reference 6.4; stdlib uses `dynamic T` | Collection surfaces still need self-host compiler-grade APIs |
| Memory contracts | `where disjoint`, `where overlap`, `where same`, `if disjoint`, `unsafe assume disjoint` in grammar/docs/source | Alias proof carriers are the first-class compile-time artifact; package-image and SSA validation paths are implemented in the host compiler |
| Unsafe/FFI/asm | Grammar and stdlib platform modules | Self-host compiler should keep unsafe boundaries small and auditable |
| Loop behavior | `infinite`, `non-deterministic`, `willexit`, `independent` | Ported compiler loops need explicit annotations |

## Non-gaps With Large Translation Cost

| Topic | Why It Is Not A Gap | Porting Cost |
|---|---|---|
| No garbage collector | Stark intentionally uses ownership, move, drop, and borrow checking | Compiler IR graphs use the resolved OQ-16 model: arena/table storage with typed handles, first-class fact tables, and fact-transfer validation. Direct C# object graph translation is rejected. |
| No exceptions | Stark intentionally uses values for recoverable errors | Requires broad error-value conventions and possible `trap` policy for invariants |
| Explicit storage classes | Stark declarations use explicit storage classes such as `stack`, `heap`, `register`, `static`, and `arena` where valid | Mechanical rewrite burden for every C# local |
| No trait objects | Stark traits/doctrines are compile-time contracts | Compiler abstractions should prefer closed-world generic functions/modules over runtime dispatch |
