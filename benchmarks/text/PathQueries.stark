import System.IO.Path
import System.Text
module Benchmarks.Text.PathQueries

export unsafe ffi fn i32[min max] main() {
    stack mut i64[min max] checksum = 0;

    for willexit (stack mut u16[0 10000] i = 0; i < 10000; i += 1) {
        checksum += System.Text.AsciiLength(System.IO.Path.Extension("alpha/beta.txt"));
        checksum += System.Text.AsciiLength(System.IO.Path.BaseName("alpha/beta.txt"));
        checksum += System.Text.AsciiLength(System.IO.Path.DirectoryName("alpha/beta.txt"));
        checksum += System.Text.AsciiLength(System.IO.Path.Extension("archive.tar.gz"));
        checksum += System.Text.AsciiLength(System.IO.Path.BaseName("archive.tar.gz"));
        checksum += System.Text.AsciiLength(System.IO.Path.DirectoryName("archive.tar.gz"));
    }

    if (checksum != 270000) {
        return 1;
    }

    return 0;
}
