import System.Experimental.Text
module Benchmarks.Text.ExperimentalTextConcatCopy

const Iterations = 5000;

fn i64[min max] AsciiChecksum(borrow Ascii text) {
    return text.Length
        + (i64[min max])(*text.Data)
        + (i64[min max])(*(&text.Data[text.Length - 1]));
}

fn i64[min max] UnicodeChecksum(borrow Unicode text) {
    return text.Length
        + (i64[min max])(*text.Data)
        + (i64[min max])(*(&text.Data[text.Length - 1]));
}

export ffi fn i32[min max] main() {
    stack mut i8[-128 127][32] asciiStorage;
    stack mut Ascii asciiText = new Ascii() {
        Data = &asciiStorage[0],
        Length = 0,
        Capacity = 32
    };
    stack mut i32[-2147483648 2147483647][32] unicodeStorage;
    stack mut Unicode unicodeText = new Unicode() {
        Data = &unicodeStorage[0],
        Length = 0,
        Capacity = 32
    };
    stack mut i64[min max] checksum = 0;

    for willexit (stack mut i32[0 5000] i = 0; i < Iterations; i += 1) {
        if (!System.Experimental.Text.TryConcatAscii(&asciiText, "prefix/", "body")) {
            return 1;
        }
        checksum += AsciiChecksum(asciiText);

        if (!System.Experimental.Text.TryConcatUnicode(&unicodeText, (unicode)"Value:", (unicode)"body")) {
            return 2;
        }
        checksum += UnicodeChecksum(unicodeText);
    }

    if (checksum != 2305000) {
        return 3;
    }

    return 0;
}
