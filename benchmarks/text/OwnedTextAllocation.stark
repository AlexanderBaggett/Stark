import System.Text
import System.Memory
module Benchmarks.Text.OwnedTextAllocation

unsafe fn bool AddAsciiLength(System.Memory.MemoryResult<System.Text.OwnedAscii> result, rawmutptr<i64[min max]> checksum) {
    switch (result) {
        case System.Memory.MemoryResult<System.Text.OwnedAscii>.Ok(var value):
            *checksum += value.Length();
            return true;
        case System.Memory.MemoryResult<System.Text.OwnedAscii>.Err(var error):
            return false;
    }
}

unsafe fn bool AddUnicodeLength(System.Memory.MemoryResult<System.Text.OwnedUnicode> result, rawmutptr<i64[min max]> checksum) {
    switch (result) {
        case System.Memory.MemoryResult<System.Text.OwnedUnicode>.Ok(var value):
            *checksum += value.Length();
            return true;
        case System.Memory.MemoryResult<System.Text.OwnedUnicode>.Err(var error):
            return false;
    }
}

export unsafe ffi fn i32[min max] main() {
    stack mut i64[min max] checksum = 0;

    for willexit (stack mut u16[0 1000] i = 0; i < 1000; i += 1) {
        stack i32[min max] value = i;
        if (!AddAsciiLength(System.Text.ToAscii(value), &checksum)) {
            return 1;
        }

        if (!AddUnicodeLength(System.Text.ToUnicode(value), &checksum)) {
            return 2;
        }

        if (!AddAsciiLength(System.Text.ConcatAscii("Score: ", 7, System.Text.ToAscii(value)), &checksum)) {
            return 3;
        }

        if (!AddUnicodeLength(System.Text.ConcatUnicode((unicode)"Score: ", 7, System.Text.ToUnicode(value)), &checksum)) {
            return 4;
        }
    }

    if (checksum != 25560) {
        return 5;
    }

    return 0;
}
