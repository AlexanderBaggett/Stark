import System.Experimental.Collections
import System.Memory
module Benchmarks.Collections.ExperimentalDictionaryInsert

finite law bool Ok(MemoryStatus status) {
    switch (status) {
        case MemoryStatus.Ok:
            return true;
        case MemoryStatus.Err(var error):
            return false;
    }
}

export ffi fn i32[min max] main() {
    stack mut System.Experimental.Collections.Dictionary<i32[0 max], i32[0 max]> values = new();
    if (!Ok(values.Reserve(8192))) {
        return 1;
    }

    stack mut i64[min max] checksum = 0;
    for willexit (stack mut i32[0 8192] i = 0; i < 8192; i += 1) {
        stack i32[0 max] value = (i32[0 max])(i * 7);
        if (!Ok(values.Set(i, value))) {
            return 2;
        }

        checksum += (i64[min max])value;
    }

    stack mut i32[0 max] found = 0;
    stack i32[0 max] lastKey = 8191;
    if (values.Count() != 8192 || !values.TryGet(lastKey, found) || found != 57337) {
        return 3;
    }

    if (checksum != 234852352) {
        return 4;
    }

    return 0;
}
