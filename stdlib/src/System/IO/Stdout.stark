module System.IO.Stdout

ffi fn i32 fputs(ascii text, rawptr<i8> stream);
const rawptr<i8> stdout = null;

public fn void Write(ascii text) {
    fputs(text, stdout);
    return;
}

public fn void WriteLine(ascii text) {
    Write(text);
    Write("\n");
    return;
}
