import System.Runtime.Platform.Linux
import System.Runtime.Platform.Windows
module System.Runtime.Platform

internal fn void ExitProcess(i32[min max] code) {
    unsafe {
        System.Runtime.Platform.Linux.ExitProcess(code);
        return;
    }
}

internal fn i32[min max] ProcessId() {
    return System.Runtime.Platform.Linux.ProcessId();
}

internal fn i32[min max] WriteStdoutAscii(ascii text) {
    return System.Runtime.Platform.Linux.WriteStdoutAscii(text);
}

internal fn i32[min max] WriteStdoutUnicode(unicode text) {
    return System.Runtime.Platform.Linux.WriteStdoutUnicode(text);
}

internal unsafe fn i32[min max] WriteStdoutBytes(rawptr<i8[min max]>[length] data, u64[0 2 ** 63 - 1] length) {
    return System.Runtime.Platform.Linux.WriteStdoutBytes(data, length);
}

internal fn i32[min max] WriteStdoutAsciiLine(ascii text) {
    return System.Runtime.Platform.Linux.WriteStdoutAsciiLine(text);
}

internal fn i32[min max] WriteStdoutUnicodeLine(unicode text) {
    return System.Runtime.Platform.Linux.WriteStdoutUnicodeLine(text);
}

internal unsafe fn i32[min max] WriteStdoutBytesLine(rawptr<i8[min max]>[length] data, u64[0 2 ** 63 - 1] length) {
    return System.Runtime.Platform.Linux.WriteStdoutBytesLine(data, length);
}

internal fn i32[min max] WriteStderrAscii(ascii text) {
    return System.Runtime.Platform.Linux.WriteStderrAscii(text);
}

internal fn i32[min max] WriteStderrUnicode(unicode text) {
    return System.Runtime.Platform.Linux.WriteStderrUnicode(text);
}

internal unsafe fn i32[min max] WriteStderrBytes(rawptr<i8[min max]>[length] data, u64[0 2 ** 63 - 1] length) {
    return System.Runtime.Platform.Linux.WriteStderrBytes(data, length);
}

internal fn i32[min max] WriteStderrAsciiLine(ascii text) {
    return System.Runtime.Platform.Linux.WriteStderrAsciiLine(text);
}

internal fn i32[min max] WriteStderrUnicodeLine(unicode text) {
    return System.Runtime.Platform.Linux.WriteStderrUnicodeLine(text);
}

internal unsafe fn i32[min max] WriteStderrBytesLine(rawptr<i8[min max]>[length] data, u64[0 2 ** 63 - 1] length) {
    return System.Runtime.Platform.Linux.WriteStderrBytesLine(data, length);
}

internal unsafe fn rawptr<i8[min max]> OpenStdin() {
    return System.Runtime.Platform.Linux.OpenStdin();
}

internal unsafe fn void CloseStdin(rawptr<i8[min max]> handle) {
    System.Runtime.Platform.Linux.CloseStdin(handle);
    return;
}

internal unsafe fn i64[min max] ReadStdin(rawmutptr<i8[min max]>[capacity] buffer, u64[0 2 ** 63 - 1] capacity) {
    return System.Runtime.Platform.Linux.ReadStdin(buffer, capacity);
}

internal unsafe inline fn i32[min max] WriteAscii(rawptr<i8[min max]> handle, ascii text) {
    return System.Runtime.Platform.Linux.WriteAscii(handle, text);
}

internal inline finite law ascii DirectorySeparator() {
    return System.Runtime.Platform.Linux.DirectorySeparator();
}

internal inline finite law ascii AlternateDirectorySeparator() {
    return System.Runtime.Platform.Linux.AlternateDirectorySeparator();
}

internal inline finite law ascii PathSeparator() {
    return System.Runtime.Platform.Linux.PathSeparator();
}

internal inline finite law bool IsDirectorySeparator(ascii value) {
    return System.Runtime.Platform.Linux.IsDirectorySeparator(value);
}

internal unsafe fn bool IsTerminal(rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.IsTerminal(handle);
}

internal unsafe inline fn i32[min max] WriteUnicode(rawptr<i8[min max]> handle, unicode text) {
    return System.Runtime.Platform.Linux.WriteUnicode(handle, text);
}

internal unsafe fn rawptr<i8[min max]> OpenFileRead(ascii path) {
    return System.Runtime.Platform.Linux.OpenFileRead(path);
}

internal unsafe fn rawptr<i8[min max]> OpenFileWrite(ascii path) {
    return System.Runtime.Platform.Linux.OpenFileWrite(path);
}

internal unsafe fn rawptr<i8[min max]> OpenFileAppend(ascii path) {
    return System.Runtime.Platform.Linux.OpenFileAppend(path);
}

internal unsafe fn rawptr<i8[min max]> OpenFileReadWrite(ascii path) {
    return System.Runtime.Platform.Linux.OpenFileReadWrite(path);
}

internal unsafe inline fn i32[min max] CloseFile(rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.CloseFile(handle);
}

internal unsafe fn i32[min max] FlushFile(rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.FlushFile(handle);
}

internal unsafe inline fn i64[min max] ReadFile(rawmutptr<i8[min max]> buffer, i64[min max] size, i64[min max] count, rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.ReadFile(buffer, size, count, handle);
}

internal unsafe inline fn i64[min max] WriteFile(rawptr<i8[min max]> buffer, i64[min max] size, i64[min max] count, rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.WriteFile(buffer, size, count, handle);
}

internal unsafe inline fn i64[min max] ReadFileBytes(rawmutptr<i8[min max]>[length] buffer, u64[0 2 ** 63 - 1] length, rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.ReadFileBytes(buffer, length, handle);
}

internal unsafe inline fn i64[min max] WriteFileBytes(rawptr<i8[min max]>[length] buffer, u64[0 2 ** 63 - 1] length, rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.WriteFileBytes(buffer, length, handle);
}

internal unsafe inline fn i64[min max] ReadFileBytesFast(rawmutptr<i8[min max]>[length] buffer, u64[0 2 ** 63 - 1] length, rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.ReadFileBytes(buffer, length, handle);
}

internal unsafe inline fn i64[min max] WriteFileBytesFast(rawptr<i8[min max]>[length] buffer, u64[0 2 ** 63 - 1] length, rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.WriteFileBytes(buffer, length, handle);
}

internal unsafe inline fn i64[min max] SeekFile(rawptr<i8[min max]> handle, i64[min max] offset, i32[min max] origin) {
    return System.Runtime.Platform.Linux.SeekFile(handle, offset, origin);
}

internal fn i32[min max] DeleteFile(ascii path) {
    return System.Runtime.Platform.Linux.DeleteFile(path);
}

internal fn i32[min max] MoveFile(ascii oldPath, ascii newPath) {
    return System.Runtime.Platform.Linux.MoveFile(oldPath, newPath);
}

internal fn bool FileExists(ascii path) {
    return System.Runtime.Platform.Linux.FileExists(path);
}

internal fn bool PathExists(ascii path) {
    return System.Runtime.Platform.Linux.PathExists(path);
}

internal fn bool IsFile(ascii path) {
    return System.Runtime.Platform.Linux.IsFile(path);
}

internal fn bool IsDirectory(ascii path) {
    return System.Runtime.Platform.Linux.IsDirectory(path);
}

internal fn i32[min max] CreateDirectory(ascii path) {
    return System.Runtime.Platform.Linux.CreateDirectory(path);
}

internal fn i32[min max] DeleteDirectory(ascii path) {
    return System.Runtime.Platform.Linux.DeleteDirectory(path);
}

internal unsafe fn rawptr<i8[min max]> OpenDirectory(ascii path)
{
    return System.Runtime.Platform.Linux.OpenDirectory(path);
}

internal unsafe fn rawptr<i8[min max]> OpenDirectoryFast(ascii path)
{
    return System.Runtime.Platform.Linux.OpenDirectory(path);
}

internal unsafe inline fn rawptr<i8[min max]> OpenDirectoryFastInto(
    ascii path,
    rawmutptr<i8[min max]>[capacity] buffer,
    u64[0 2 ** 63 - 1] capacity,
    rawmutptr<i64[min max]> initialLength)
{
    return System.Runtime.Platform.Linux.OpenDirectoryInto(path, buffer, capacity, initialLength);
}

internal inline fn u64[0 2 ** 63 - 1] DirectoryBufferCapacity() {
    return System.Runtime.Platform.Linux.DirectoryBufferCapacity();
}

internal unsafe inline fn i32[min max] CloseDirectory(rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.CloseDirectory(handle);
}

internal unsafe inline fn i32[min max] ReadDirectoryEntry(
    rawptr<i8[min max]> handle,
    rawmutptr<i8[min max]>[capacity] buffer,
    u64[0 2 ** 63 - 1] capacity,
    rawmutptr<i64[min max]> readPos,
    rawmutptr<i64[min max]> readLength,
    rawmutptr<Ascii> name,
    rawmutptr<i32[min max]> kind)
{
    return System.Runtime.Platform.Linux.ReadDirectoryEntry(handle, buffer, capacity, readPos, readLength, name, kind);
}

internal unsafe inline fn i32[min max] ReadDirectoryEntryInfo(
    rawptr<i8[min max]> handle,
    rawmutptr<i8[min max]>[capacity] buffer,
    u64[0 2 ** 63 - 1] capacity,
    rawmutptr<i64[min max]> readPos,
    rawmutptr<i64[min max]> readLength,
    rawmutptr<i64[min max]> nameLength,
    rawmutptr<i32[min max]> kind)
{
    return System.Runtime.Platform.Linux.ReadDirectoryEntryInfo(handle, buffer, capacity, readPos, readLength, nameLength, kind);
}

internal unsafe inline fn i64[min max] ReadDirectoryNameLengthChecksum(
    rawptr<i8[min max]> handle,
    rawmutptr<i8[min max]>[capacity] buffer,
    u64[0 2 ** 63 - 1] capacity,
    rawmutptr<i64[min max]> readPos,
    rawmutptr<i64[min max]> readLength,
    i64[min max] expectedCount)
{
    return System.Runtime.Platform.Linux.ReadDirectoryNameLengthChecksum(handle, buffer, capacity, readPos, readLength, expectedCount);
}

internal unsafe fn bool TryCurrentDirectory(rawmutptr<Ascii> destination) {
    return System.Runtime.Platform.Linux.TryCurrentDirectory(destination);
}

internal unsafe fn rawptr<i8[min max]> StartThread(fnptr<fn i32[min max]()> entry) {
    return System.Runtime.Platform.Linux.StartThread(entry);
}

internal unsafe fn i32[min max] JoinThread(rawptr<i8[min max]> handle, rawmutptr<i32[min max]> exitCode) {
    return System.Runtime.Platform.Linux.JoinThread(handle, exitCode);
}

internal unsafe fn i32[min max] DetachThread(rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.DetachThread(handle);
}

internal unsafe fn rawptr<i8[min max]> ConnectTcpIPv4(
    u8[0 max] a,
    u8[0 max] b,
    u8[0 max] c,
    u8[0 max] d,
    u16[0 max] port,
    rawmutptr<i32[min max]> status)
{
    return System.Runtime.Platform.Linux.ConnectTcpIPv4(a, b, c, d, port, status);
}

internal unsafe fn rawptr<i8[min max]> ListenTcpIPv4(
    u8[0 max] a,
    u8[0 max] b,
    u8[0 max] c,
    u8[0 max] d,
    u16[0 max] port,
    rawmutptr<i32[min max]> status)
{
    return System.Runtime.Platform.Linux.ListenTcpIPv4(a, b, c, d, port, status);
}

internal unsafe fn i64[min max] ReadSocket(
    rawptr<i8[min max]> handle,
    rawmutptr<i8[min max]>[capacity] buffer,
    u64[0 2 ** 63 - 1] capacity)
{
    return System.Runtime.Platform.Linux.ReadSocket(handle, buffer, capacity);
}

internal unsafe fn i64[min max] WriteSocket(
    rawptr<i8[min max]> handle,
    rawptr<i8[min max]>[length] buffer,
    u64[0 2 ** 63 - 1] length)
{
    return System.Runtime.Platform.Linux.WriteSocket(handle, buffer, length);
}

internal unsafe fn i64[min max] ReadSocketVector2(
    rawptr<i8[min max]> handle,
    rawmutptr<i8[min max]>[firstCapacity] firstBuffer,
    u64[0 2 ** 63 - 1] firstCapacity,
    rawmutptr<i8[min max]>[secondCapacity] secondBuffer,
    u64[0 2 ** 63 - 1] secondCapacity)
{
    return System.Runtime.Platform.Linux.ReadSocketVector2(handle, firstBuffer, firstCapacity, secondBuffer, secondCapacity);
}

internal unsafe fn i64[min max] WriteSocketVector2(
    rawptr<i8[min max]> handle,
    rawptr<i8[min max]>[firstLength] firstBuffer,
    u64[0 2 ** 63 - 1] firstLength,
    rawptr<i8[min max]>[secondLength] secondBuffer,
    u64[0 2 ** 63 - 1] secondLength)
{
    return System.Runtime.Platform.Linux.WriteSocketVector2(handle, firstBuffer, firstLength, secondBuffer, secondLength);
}

internal unsafe fn rawptr<i8[min max]> AcceptSocket(
    rawptr<i8[min max]> handle,
    rawmutptr<i32[min max]> status)
{
    return System.Runtime.Platform.Linux.AcceptSocket(handle, status);
}

internal unsafe fn i32[min max] ShutdownSocket(rawptr<i8[min max]> handle, i32[min max] how) {
    return System.Runtime.Platform.Linux.ShutdownSocket(handle, how);
}

internal unsafe fn i32[min max] CloseSocket(rawptr<i8[min max]> handle) {
    return System.Runtime.Platform.Linux.CloseSocket(handle);
}

internal fn void YieldThread() {
    System.Runtime.Platform.Linux.YieldThread();
    return;
}

internal fn void SleepThreadMilliseconds(u64[0 2 ** 63 - 1] milliseconds) {
    System.Runtime.Platform.Linux.SleepThreadMilliseconds(milliseconds);
    return;
}

internal unsafe fn i32[min max] WaitReadable(rawptr<i8[min max]> handle, i32[min max] timeoutMilliseconds) {
    return System.Runtime.Platform.Linux.WaitReadable(handle, timeoutMilliseconds);
}

internal unsafe fn i32[min max] WaitWritable(rawptr<i8[min max]> handle, i32[min max] timeoutMilliseconds) {
    return System.Runtime.Platform.Linux.WaitWritable(handle, timeoutMilliseconds);
}

internal unsafe fn i32[min max] FutexWait(rawptr<i32[min max]> address, i32[min max] expected) {
    return System.Runtime.Platform.Linux.FutexWait(address, expected);
}

internal unsafe fn i32[min max] FutexWake(rawptr<i32[min max]> address, i32[min max] count) {
    return System.Runtime.Platform.Linux.FutexWake(address, count);
}
