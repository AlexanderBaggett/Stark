module Book.Negative.FnptrKindMismatch

fn i32[min max] Plain() {
    return 1;
}

export unsafe ffi fn i32[min max] main() {
    stack fnptr<finite law i32[min max]()> needsStrongGuarantee = Plain;
    return needsStrongGuarantee();
}
