module Book.Negative.UseAfterMove

struct Box {
    i32[min max] Value;
}

fn void Consume(Box value) {
    return;
}

fn i32[min max] UseAfterMove() {
    stack Box box = new Box() {
        Value = 1
    };

    Consume(box);
    return box.Value;
}
