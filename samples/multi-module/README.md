# Multi-Module Sample

Compile and run the app directly from source:

```bash
dotnet run --project src -- samples/multi-module/App.stark --emit-exe -o samples/multi-module/app
./samples/multi-module/app
```
